server:
  port: 8083
  servlet:
    context-path: /scheduling

spring:
  application:
    name: scheduling-service
  datasource:
    # url: "jdbc:postgresql://dpg-cvgkvannoe9s73ce60u0-a.singapore-postgres.render.com:5432/appointment_scheduling_swre"
    # username: appointment_scheduling_swre_user
    # password: yEf1lc3NFAVyNIt5Ae0Fq43Arg5S0Jdn
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: postgres
    password: 50994
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  logging:
    level:
      org.springframework.web: DEBUG
      org.springframework.boot: DEBUG
